# Gemini (Google AI Studio)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# BrainRot API
# If the BrainRot API requires a key, put it here. If not, leave blank
# and lib/brainrotApi.ts will use its local fallback.
BRAINROT_API_URL=
BRAINROT_API_KEY=

# Imgflip — uses username/password, not an API key
IMGFLIP_USERNAME= YnotCrackedAcc
IMGFLIP_PASSWORD= Jh!Z5T62yTXCHzA

# Set to "1" to force all external APIs to use mock data (useful for demo safety)
USE_MOCKS=0
